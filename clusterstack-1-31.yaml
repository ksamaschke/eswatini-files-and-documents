apiVersion: clusterstack.x-k8s.io/v1alpha1
kind: ClusterStack
metadata:
  name: docker-1-31
  namespace: cluster
spec:
  provider: docker
  name: scs
  kubernetesVersion: "1.31"
  channel: custom
  autoSubscribe: false
  noProvider: true
  versions:
    - v0-sha.hdl6pjy
